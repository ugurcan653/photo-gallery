import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nl.lely.mobile.assistantNext',
  appName: 'photo-gallery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
